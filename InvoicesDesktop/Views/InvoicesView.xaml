<UserControl x:Class="InvoicesDesktop.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:InvoicesDesktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:InvoicesViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Invoices"
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}" />
                <TextBlock Text="Browse issued invoices and create new ones"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"
                           Opacity="0.6" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                        HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding RefreshCommand}"
                        ToolTip="Refresh" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center" />
                        <TextBlock Text="Refresh" Margin="8,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding CreateCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" />
                        <TextBlock Text="New invoice" Margin="8,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </DockPanel>

        <materialDesign:Card Grid.Row="1" Padding="0" UniformCornerRadius="10"
                             materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <DataGrid ItemsSource="{Binding Invoices}"
                          SelectedItem="{Binding SelectedInvoice}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          materialDesign:DataGridAssist.CellPadding="16 10 16 10"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="16 10 16 10"
                          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Number" Binding="{Binding InvoiceNumber}" Width="Auto" />
                        <DataGridTextColumn Header="Contractor" Binding="{Binding Contractor.ContractorName}" Width="2*" />
                        <DataGridTextColumn Header="Invoice date" Binding="{Binding InvoiceDate, StringFormat=d}" Width="Auto" />
                        <DataGridTextColumn Header="Due date" Binding="{Binding DueDate, StringFormat=d}" Width="Auto" />
                        <DataGridTextColumn Header="Net" Binding="{Binding NetValue, StringFormat=N2}" Width="Auto">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Gross" Binding="{Binding GrossValue, StringFormat=N2}" Width="Auto">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Currency" Binding="{Binding Currency}" Width="Auto" />
                        <DataGridTemplateColumn Header="Status" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" Padding="10,3" HorizontalAlignment="Left"
                                            Background="{Binding Paid, Converter={StaticResource PaidToBrushConverter}}">
                                        <TextBlock Text="{Binding Paid, Converter={StaticResource PaidToTextConverter}}"
                                                   Foreground="White" FontSize="11" FontWeight="SemiBold" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Actions" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Height="36" Width="36"
                                                ToolTip="Edit"
                                                Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="Pencil" />
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Height="36" Width="36"
                                                ToolTip="Delete"
                                                Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="DeleteOutline" />
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <materialDesign:Card VerticalAlignment="Center" HorizontalAlignment="Center"
                                     Padding="24,16" UniformCornerRadius="8"
                                     Visibility="{Binding Invoices.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="FileDocumentMultiple" Width="40" Height="40"
                                                 HorizontalAlignment="Center" Opacity="0.4" />
                        <TextBlock Text="No invoices yet"
                                   Margin="0,8,0,0" Opacity="0.6"
                                   Style="{StaticResource MaterialDesignBody1TextBlock}" />
                    </StackPanel>
                </materialDesign:Card>

                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True"
                             Width="42" Height="42"
                             VerticalAlignment="Center" HorizontalAlignment="Center"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
