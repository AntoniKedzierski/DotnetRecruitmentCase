<Application x:Class="InvoicesDesktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:InvoicesDesktop"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:InvoicesDesktop.ViewModels"
             xmlns:views="clr-namespace:InvoicesDesktop.Views"
             xmlns:converters="clr-namespace:InvoicesDesktop.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light"
                                             PrimaryColor="Indigo"
                                             SecondaryColor="Pink" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Lato font embedded under Resources/Fonts/Lato -->
            <FontFamily x:Key="LatoFont">pack://application:,,,/Resources/Fonts/Lato/#Lato</FontFamily>
            <FontFamily x:Key="MaterialDesignFont">pack://application:,,,/Resources/Fonts/Lato/#Lato</FontFamily>

            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
            <converters:PaidToBrushConverter x:Key="PaidToBrushConverter" />
            <converters:PaidToTextConverter x:Key="PaidToTextConverter" />

            <!-- Map view-models to their views -->
            <DataTemplate DataType="{x:Type vm:InvoicesViewModel}">
                <views:InvoicesView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ContractorsViewModel}">
                <views:ContractorsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ItemsViewModel}">
                <views:ItemsView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
